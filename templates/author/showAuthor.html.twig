
    <h1>Détails de l'auteur : {{ author.username }}</h1>
    <img src="{{ asset(author.picture) }}" alt="Author Pictures">
    <p>Email : {{ author.email }}</p>
    <p>Nombre de livres publiés : {{ author.nb_books }}</p>
    <a href="{{ path('list_authors') }}">Retour à la liste des auteurs</a>